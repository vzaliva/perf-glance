# Ideas

1. **Grouping taxonomy as file hierarchy** — Allow new groups to be added by dropping files into a directory.
2. **Grouping UI** — Let users assign, re-assign, or clear the grouping of the selected process for future classification; e.g. pressing `g` opens a group picker.

