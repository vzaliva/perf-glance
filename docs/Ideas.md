# Ideas

3. **Grouping taxonomy as file hierarchy** — Allow new groups to be added by dropping files into a directory.
5. **Actions** — Keyboard shortcut to kill a process or entire group. (k-kill, K-kill-9)
6. **Details** — Press Enter on selection to show path, PID, and other process info.
7. **Grouping UI** — Let users assign, re-assign, or clear the grouping of the selected process for future classification; e.g. pressing `g` opens a group picker.

